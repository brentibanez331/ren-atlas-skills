# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Installed converter deps (restored via `npm install` in the skill)
node_modules/
package-lock.json

# Atlas runtime artifacts (generated into a vault, not part of the pack)
.atlas/
